class ApplicationController < ActionController::Base
  # We will define a method in here, where we want to send the request to.
    # Within the routes.rb file, to specify a METHOD, is that you put a # sign AFTER the controller name, and then specify the name of the method
    def home
    end
end
